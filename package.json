{
  "name": "substrate-css",
  "version": "0.0.2",
  "description": "CSS UI kit that provides solid components, leaving it to you to add the visual flair",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jakob Skjerning",
  "license": "MIT",
  "dependencies": {
    "normalize.css": "^7.0.0"
  },
  "devDependencies": {
    "kss": "^3.0.0-beta.18"
  }
}
