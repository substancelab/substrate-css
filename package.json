{
  "name": "aethercss-core",
  "version": "0.0.1",
  "description": "Core components of AetherCSS. Parts of the stylesheet that aren't really useful on their own",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jakob Skjerning",
  "license": "MIT",
  "dependencies": {
    "normalize.css": "^7.0.0",
    "system-font-css": "^2.0.1"
  }
}
